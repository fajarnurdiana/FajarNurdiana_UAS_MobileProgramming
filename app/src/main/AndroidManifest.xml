<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/applogo"
        android:label="Uas Mobile Programming"
        android:roundIcon="@drawable/applogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.fajarnurdiana_uas.UpdateActivity"></activity>
        <activity android:name="com.example.fajarnurdiana_uas.DetailActivity" />
        <activity android:name="com.example.fajarnurdiana_uas.CreateActivity" />
        <activity android:name="com.example.fajarnurdiana_uas.LocalActivity" />
        <activity android:name="com.example.fajarnurdiana_uas.SmsActivity" />
        <activity android:name="com.example.fajarnurdiana_uas.AsyntaskActivity" />
        <activity android:name="com.example.fajarnurdiana_uas.NetworkActivity" />
        <activity android:name="com.example.fajarnurdiana_uas.HomeActivity" />
        <activity android:name="com.example.fajarnurdiana_uas.LoginActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyB5Jh9erzrnMT47082qSs5z2-HUJOtoZ4s" />

        <activity
            android:name="com.example.fajarnurdiana_uas.MapsActivity"
            android:exported="false"
            android:label="@string/title_activity_maps" />
        <activity
            android:name="com.example.fajarnurdiana_uas.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>